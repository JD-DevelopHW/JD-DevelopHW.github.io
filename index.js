tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#ffd900', // Custom Gold from Theme
            secondary: '#25D366', // WhatsApp Green
            darkBg: '#0a0e17', // Deep Navy/Black Background
            cardBg: '#161b26', // Slightly lighter navy for cards
          },
          fontFamily: {
            sans: ['Plus Jakarta Sans', 'sans-serif'],
          },
          borderRadius: {
            'custom': '8px', // ROUND_EIGHT from Theme
          }
        }
      }
    }